# Deploy config from git to VyOS

First you have to log into a VyOS installation and `git clone` this repo into `/config/scripts/`. After this run the script `sudo /config/scripts/network/vyos/scripts/setup.sh`.
